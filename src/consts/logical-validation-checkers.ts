/** Used with `checkAllOf` and `checkAnyOf` as a special `validationError` to indicate that the first validation error, from the ordered
 * checkers list, should be used */
export const USE_FIRST_ERROR = 'use-first-error';

/** Used with `checkAllOf` and `checkAnyOf` as a special `validationError` to indicate that the last validation error, from the ordered
 * checkers list, should be used */
export const USE_LAST_ERROR = 'use-last-error';
